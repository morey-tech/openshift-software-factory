# OpenShift GitOps

Red Hat OpenShift GitOps (Argo CD) — the GitOps engine that drives the entire software factory deployment.

- `operator/` — OperatorGroup and Subscription in `openshift-gitops-operator` (default instance disabled)
- `instance/` — ArgoCD CR (`software-factory-argocd`) and ClusterRole in `openshift-gitops`
