# External DNS (Optional)

Automated DNS record management for cluster services.

- `operator/` — Subscription and OperatorGroup for the External DNS operator
- `instance/` — DNS provider configuration
