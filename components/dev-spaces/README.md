# Dev Spaces

Red Hat OpenShift Dev Spaces — browser-based, containerized cloud development environments.

- `operator/` — Subscription for the Dev Spaces operator in `openshift-operators` (no OperatorGroup needed)
- `instance/` — CheCluster CR, VS Code editor config, and Kustomization in `openshift-devspaces`
