apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: job-rhdh-secrets-init
    options:
      annotations:
        argocd.argoproj.io/sync-wave: "-2"
    files:
      - rhdh-secrets-init-job.sh

resources:
  - rhdh-secrets-init-job.yaml
  - dynamic-plugins-root-pvc.yaml
  - dynamic-plugins-rhdh.yaml
  - app-config-rhdh.yaml
  - backstage.yaml
