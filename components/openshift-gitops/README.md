# OpenShift GitOps

Red Hat OpenShift GitOps (Argo CD) — the GitOps engine that drives the entire software factory deployment.

- `operator/` — Subscription and OperatorGroup for the GitOps operator
- `instance/` — ArgoCD CR, RBAC (ClusterRoleBindings), and AppProject definitions
